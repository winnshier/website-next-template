# 正式环境配置

# 环境标识
NODE_ENV=production
NEXT_PUBLIC_ENV=production

# 站点配置
NEXT_PUBLIC_SITE_URL=https://example.com

# CDN配置（可选）
# 如果不使用CDN，注释掉以下配置，静态文件将从主域名加载
NEXT_PUBLIC_CDN_URL=https://cdn.example.com
# IMAGE_CDN_TOKEN=production_cdn_token_here

# CDN上传配置（可选，scripts/upload-static.sh 使用）
# 配置后部署时会自动通过 Docker 容器上传静态文件到对象存储
# 注意：仅当 CDN_UPLOAD_ENABLED=true 且配置了对应提供商的认证信息时才会执行上传
# CDN_UPLOAD_ENABLED=true
# CDN_PROVIDER=aliyun
# CDN_SOURCE_DIR=.next/static
# CDN_TARGET_PREFIX=_next/static
# CDN_UPLOAD_JOBS=8
# CDN_SKIP_ON_ERROR=false
# CDN_DRY_RUN=false

# public 目录上传配置（可选）
# 是否上传 public 目录到 CDN（默认 false）
# 开启后会将 public 目录的静态资源（图片、视频等）上传到 CDN
# CDN_UPLOAD_PUBLIC=false
# CDN_PUBLIC_SOURCE_DIR=public
# CDN_PUBLIC_TARGET_PREFIX=public

# 阿里云 OSS 凭证
# OSS_BUCKET=oss://your-bucket-name
# OSS_ACCESS_KEY_ID=your_access_key_id
# OSS_ACCESS_KEY_SECRET=your_access_key_secret
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
# OSS_STS_TOKEN=

# 腾讯云 COS 凭证
# COS_BUCKET=your-bucket-1250000000
# COS_REGION=ap-guangzhou
# COS_SECRET_ID=your_secret_id
# COS_SECRET_KEY=your_secret_key
# COS_SESSION_TOKEN=
# COS_SCHEME=https

# CDN提供商说明：
# - aliyun: 使用 ossutil 官方 Docker 镜像上传到 OSS（无需本地安装）
# - tencent: 使用 docker/cos-upload.Dockerfile 构建的 coscmd 镜像上传到 COS（无需本地安装）
# 默认值：aliyun

# API配置
NEXT_PUBLIC_API_URL=https://api.example.com
API_SECRET_KEY=production_api_secret_key_here

# 调试选项
NEXT_PUBLIC_SHOW_DEBUG_INFO=false
NEXT_PUBLIC_DEBUG=false

# 可选：数据库配置
# DATABASE_URL=postgresql://user:password@db.example.com:5432/webtem_production

# 可选：第三方服务
# GOOGLE_ANALYTICS_ID=G-PRODUCTION-XXXXXXXXXX
# SENTRY_DSN=https://xxx@sentry.io/production
